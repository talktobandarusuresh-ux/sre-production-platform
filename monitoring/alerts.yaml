groups:
- name: sre-alerts

  rules:

  - alert: HighCPU

    expr: sum(rate(container_cpu_usage_seconds_total[5m])) > 0.8

    for: 2m

    labels:
      severity: critical

    annotations:
      description: High CPU usage detected